server {
    listen 80;
    server_name exams.university.dz;  # Change to your domain

    # Redirect HTTP to HTTPS (uncomment when SSL is configured)
    # return 301 https://$server_name$request_uri;

    # Frontend static files
    root /opt/smartgrader/frontend/dist;
    index index.html;

    # API proxy
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 50M;
    }

    # SPA fallback — serve index.html for all non-API, non-asset routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# HTTPS configuration (uncomment after running certbot)
# server {
#     listen 443 ssl http2;
#     server_name exams.university.dz;
#
#     ssl_certificate /etc/letsencrypt/live/exams.university.dz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/exams.university.dz/privkey.pem;
#
#     # ... same location blocks as above ...
# }
